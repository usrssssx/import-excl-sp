<!doctype html>
<html lang="ru">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Контекст Bitrix24 не найден</title>
    <style>
        body {
            font-family: "Segoe UI", Arial, sans-serif;
            background: #f5f7fb;
            display: grid;
            place-items: center;
            min-height: 100vh;
            margin: 0;
            color: #173152;
        }
        .box {
            background: #fff;
            border: 1px solid #d9e3f0;
            border-radius: 12px;
            padding: 20px;
            width: min(560px, 92vw);
        }
        code { background: #eef3fa; padding: 2px 6px; border-radius: 6px; }
    </style>
</head>
<body>
    <div class="box">
        <h2>Не найден контекст Bitrix24</h2>
        <p>Откройте приложение из интерфейса Bitrix24 (через пункт меню приложения), чтобы передались параметры <code>AUTH_ID</code>, <code>DOMAIN</code> и <code>USER_ID</code>.</p>
    </div>
</body>
</html>
